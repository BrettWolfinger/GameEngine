# Runtime Config System — Roadmap

Implementation complete (all three phases). See [`docs/engine/runtime-config.md`](../engine/runtime-config.md)
for the architecture reference.

Remaining work:
- Issue #83 — Consider splitting config editor into its own file
- Issue #80 — Customize look/design of docs and API sites
